# Main 接管就绪清单

## 目标

在正式让 `main` 长期承接 GitHub / ClawHub / 飞书知识库工作前，先确认所有关键条件已经到位。

## 一、入口与角色

- 已有正式协作群
- 正式协作群成员已确认：
  - 你
  - `main`
  - `小果`
  - `果爸`
- 已明确：
  - `main` 负责入口、判断、分流、回报
  - `小果` 是 `media` 的飞书机器人名字
  - `果爸` 负责国内平台发布执行

## 二、默认项目上下文

- 默认本地仓库路径已确认：
  - `/Users/ztl/Documents/New project`
- 默认 GitHub 仓库已确认：
  - `guoma970/openclaw-oss-starter`
- 默认分支已确认：
  - `main`

## 三、公开链路

- GitHub 已可用
- ClawHub 已可用
- 飞书知识库父目录已建立
- `11_OpenClaw开源项目` 目录已建立

## 四、规则库

下面这些规则已经存在并可供 `main` 执行时参考：

- 默认项目上下文
- GitHub 职责规则
- 新 skill 上线流程
- 发布前检查
- 分级规则
- 决策模板
- 公开链路执行顺序
- 最小公开同步动作集
- blocker 处理规则
- 标准回报模板

## 五、日常操作

- 已有日常短指令
- 已有发布流总控脚本
- 已有周期巡检脚本
- 已有知识库同步脚本
- 已有项目登记表模板
- 本地真实项目登记表已补齐
- 已有多项目日常检查规则
- 已有正式中枢群脱敏回报规则

## 六、接管判断

只有下面 3 项同时成立，才建议正式切换到 `main` 长期承接：

1. 入口已稳定
2. 默认项目上下文已稳定
3. 规则库已够用

进一步建议检查：

4. 项目登记表已生效
5. 中枢群脱敏回报已验证
6. 多项目检查已能按项目分段回报

## 七、推荐结论

如果本页大部分都已满足，则可以进入：

- `main` 试运行阶段
- `main` 正式承接阶段
- `main` 接管验收阶段

## 一句话规则

先确认入口、上下文、规则和公开链路都稳定，再让 `main` 长期接管。
