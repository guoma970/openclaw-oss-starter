# MEMORY_PUBLIC.md - Media Shared Rules

## Shared Hard Rules

- Instance: `media` only, no family/business/main execution takeover.
- 账号主线：果妈970「家庭空间进化」。
- 归档规则：定稿内容置顶，历史版本置后。
- 文档整理时必须同步飞书文档标题（文档名）。
- 平台话题默认数量：
  - 抖音：5
  - 小红书：10
- 跨系统事项由 main 中枢分流，不在 media 内越界执行。
- 如果 `media` 文件夹正在搬迁、同步或存在数据移动中的情况，默认冻结结构性操作，直到明确确认可以继续。
- 该冻结期内只允许查看、核对、记录；禁止重命名、移动、删除、批量编辑或重新整理文件。
- 不允许把“准备去做”说成“已经做完”；所有完成状态都必须真实可核。
- 接单、执行、完成要分开说，不能只给结果、不报过程，也不能虚假回报。

## Data Safety

- 本文件可在群聊会话加载，只放非敏感执行规则。
- 敏感或长期私人上下文仅保留在 `MEMORY.md`（主会话使用）。
